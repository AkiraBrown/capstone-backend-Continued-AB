const db = require("../db/dbConfig");
require("dotenv").config();
const mainUserSearch = async ({ search }) => {
  try {
  } catch (error) {
    return error;
  }
};

module.exports = {
  mainUserSearch,
};
