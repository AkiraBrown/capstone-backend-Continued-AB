// const supertest = require("supertest");
// const app = require("../app");

// describe("A route that searches for google shopping data", () => {
//   it("Responds with the searched data from the file system", async () => {
//     const response = await supertest(app).get("/serp/search/?search=coffee");
//     expect(response.body[0].price).toBe("$27.00");
//   });
// });
